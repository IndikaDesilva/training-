---
  - hosts: localhost
    vars:
      host: 192.168.1.100
      username: "idesilva"
      password: "Rabutan2@"
      vdom: "root"
    tasks:
      - name: create firewall policy
        fortios_config:
          action: "set"
          host: "{{ username }}"
          username: "{{ username }}"
          vdom: "{{ vdom }}"
          https: False
            